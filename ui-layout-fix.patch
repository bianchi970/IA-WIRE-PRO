diff --git a/frontend/style.css b/frontend/style.css
index 8d5422a..5cb6748 100644
--- a/frontend/style.css
+++ b/frontend/style.css
@@ -29,8 +29,8 @@ body{
 }
 
 .app{
-  height:100vh;
-  height:100dvh; /* mobile: si adatta quando appare tastiera */
+  min-height:100vh;
+  min-height:100dvh;
   display:flex;
   flex-direction:column;
   overflow:hidden;
@@ -99,8 +99,9 @@ body{
 /* MAIN */
 .main{
   flex:1;
-  min-height:0; /* flex overflow fix: consente ai figli di scrollare */
+  min-height:0;
   display:flex;
+  flex-direction:column;
   justify-content:center;
   padding:14px;
   overflow:hidden;
@@ -121,6 +122,7 @@ body{
 }
 
 .card-head{
+  flex-shrink:0;
   padding:16px 16px 10px 16px;
   border-bottom:1px solid var(--border);
 }
@@ -140,6 +142,7 @@ body{
 
 /* UPLOAD */
 .upload{
+  flex-shrink:0;
   padding:14px 16px;
   border-bottom:1px solid var(--border);
 }
@@ -198,9 +201,11 @@ body{
 /* CHAT */
 .chat{
   flex:1;
-  min-height:0; /* obbligatorio per scroll in flex column */
+  min-height:220px;
+  max-height: clamp(260px, 55vh, 780px);
   padding:14px 12px 12px 12px;
   overflow-y:auto;
+  scroll-behavior:smooth;
   display:flex;
   flex-direction:column;
   gap:10px;
@@ -305,4 +310,44 @@ body{
   .main{ padding:0; }
   .card{ border-radius:0; border-left:none; border-right:none; }
   .bubble{ max-width: 92%; }
+  /* mobile: compatta header card e upload per lasciare spazio alla chat */
+  .card-head{ padding:8px 12px 6px 12px; }
+  .card-head p{ display:none; }
+  .upload{ padding:8px 12px; }
+  .preview-wrap img{ width:60px; height:60px; }
+}
+
+@media (max-width: 480px){
+  .chat{
+    min-height:240px;
+    max-height: clamp(260px, 50vh, 560px);
+  }
+}
+
+/* ============================================================
+   LAYOUT FIX V2 — scroll chat + layout responsive
+   ============================================================
+   SCROLL:    solo #chat scrolla; header e composer restano fissi.
+   LARGHEZZA: card e composer espansi a 1400px per desktop wide.
+   ============================================================ */
+
+/* struttura colonna piena altezza */
+.app{ min-height:100vh; display:flex; flex-direction:column; }
+.main{ flex:1; min-height:0; display:flex; flex-direction:column; }
+.card{ flex:1; min-height:0; display:flex; flex-direction:column; }
+
+/* chat: flex elastico, niente cap, scroll interno */
+#chat.chat{ flex:1; min-height:0; max-height:none; overflow-y:auto; }
+
+/* composer fisso in basso */
+.composer{ position:sticky; bottom:0; z-index:10; }
+
+/* larghezza responsive (override 820px originale) */
+.card{ width:min(1400px, 94%); margin:0 auto; }
+.composer-inner{ max-width:1400px; }
+.hint{ max-width:1400px; }
+
+/* mobile ≤480px */
+@media (max-width:480px){
+  .main{ padding:0; }
 }
